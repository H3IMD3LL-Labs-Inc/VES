apiVersion: v1
kind: Service
metadata:
  name: chromadb-clusterip
spec:
  # Change to NodePort to expose outside the cluster for developers/testing
  type: ClusterIP
  selector:
    app: chromadb
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
