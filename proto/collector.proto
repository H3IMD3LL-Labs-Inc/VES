syntax = "proto3";

package log_collector;

import "common.proto";

message CollectResponse { bool accepted = 1; }

service LogCollector {
  rpc StreamLog(stream common.NormalizedLog) returns (stream CollectResponse);
}
